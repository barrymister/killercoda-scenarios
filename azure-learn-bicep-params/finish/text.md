# Parameterised and clean

You moved values to parameters, protected a secret with `@secure()`, and the
template lints and builds with no errors — the pattern for reusable, secret-safe
Bicep that AZ-104 expects.
