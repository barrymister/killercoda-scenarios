# Command fluency

You built the `az` commands for a resource group, a VM, and a role assignment —
the muscle memory AZ-104 rewards. Same idea works for storage, networking, and
policy: reach for `az <group> <cmd> --help` and read the flags.
