# Azure CLI fluency

A lot of AZ-104 is knowing the **right `az` command** for a task. You'll write the
commands for three everyday jobs into `answers.sh`. We grade the command shape
(the verb and key flags) — nothing runs against real Azure, so there's no cost and
no login. Use `az <group> <cmd> --help` if you get stuck.
