# The loop that never stops

`/root/count.sh` is meant to print `count 1` through `count 5` and exit, but it
**hangs** — it never stops. The `while` condition never becomes false. Don't run
it blindly (if you do, press **Ctrl-C** to stop it); read it first.
