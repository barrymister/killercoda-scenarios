# Arrays hold lists

An array stores many values in one variable: `fruits=(apple banana cherry)`.
You index elements, count them, and append more. Parameter expansion also
reshapes strings — lowercase them or strip prefixes without calling any tools.
You'll write a tiny script that does both.
