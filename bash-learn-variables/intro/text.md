# Variables hold values

Shell variables store text you reuse: `NAME=bench`, then `$NAME` to expand it.
Command substitution `$(...)` captures a command's output into a variable.
You'll write a tiny script that does both.
