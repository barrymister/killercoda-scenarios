# `[5: command not found`

`/root/check.sh` should write `many` or `few` depending on how many items are in
`items.txt`, but it errors out and never writes the verdict. There are five
items, so the answer should be **many**.
