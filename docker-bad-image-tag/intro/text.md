# The web container won't come up

A teammate tried to start the site with:

```
docker run -d --name web nginx:1.99
```

...and it errored out — there's no `web` container running. Move to the next
step to diagnose and get nginx up.
