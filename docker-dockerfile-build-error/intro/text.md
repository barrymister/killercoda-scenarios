# The image won't build

There's a small Python app in `/root/app` with a `Dockerfile`, but
`docker build` fails before it produces an image. The app code is fine — the
Dockerfile has a mistake. Move to the next step to build it, read the error,
and fix it.
