# Images come from registries

Before you can run a container, Docker needs the **image** — usually pulled from
a registry like Docker Hub. Images have **tags** (`name:version`). You'll pull
one and re-tag it as your own.
