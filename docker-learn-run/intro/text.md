# A container is a running image

An **image** is a packaged filesystem + command; a **container** is one running
instance of it. You'll start one, see it in the process list, read its logs, and
clean it up. Move to the next step.
