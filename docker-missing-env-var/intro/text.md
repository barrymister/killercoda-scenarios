# The `db` container keeps dying

The Postgres `db` container was started but it exits right away every time. The
image is fine and the tag is correct — something it *needs* at boot is missing.
Move to the next step.
