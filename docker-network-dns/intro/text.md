# The app can't find the database

Two containers are running — an `app` and a `db` — but the app can't reach the
database by the name `db`. Both are up; they just can't talk. Move to the next
step to see why and fix the networking.
