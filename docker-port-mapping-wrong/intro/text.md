# The site loads nothing on :8080

The `web` container is running, but `http://localhost:8080` returns nothing —
the connection is refused/empty. The container is healthy; the wiring to it is
wrong. Move to the next step.
