# "You are in 'detached HEAD' state"

There's an important `hotfix` commit, but it was made while **HEAD was detached**
— it isn't on any branch, so a `git switch main` would strand it. Get that commit
safely onto `main`.
