# Work in parallel with branches

A **branch** is a movable pointer to a line of commits. You make changes on a
branch, then **merge** them back into `main`. The repo in `/root/repo` has one
commit on `main`.
