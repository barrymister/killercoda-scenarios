# Reading history

`git log --oneline --graph` shows the shape of history; `git show <ref>` opens a
single commit; `git diff A B` compares two points (commits, branches, tags).
`git blame <file>` goes line-by-line. Good commit messages make all of this
worth reading later.
