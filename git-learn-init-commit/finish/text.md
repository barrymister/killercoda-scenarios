# The core loop

`git init` created the repo, `git status` showed an untracked file, `git add`
staged it, and `git commit` saved a snapshot with a message. `git log` shows the
history. Everything else in Git builds on this add → commit rhythm.
