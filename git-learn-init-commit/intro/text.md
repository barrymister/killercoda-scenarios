# A repo tracks your history

Git records **snapshots** of your files as commits. The loop is always: change
files, `add` what you want to save (staging), then `commit` it. There's an empty
`/root/repo` waiting. Move to the next step.
