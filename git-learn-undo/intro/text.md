# Made a mess? Roll it back

Git makes it safe to experiment because you can always return to the last commit.
You'll deliberately break a file, then discard the change with `git restore`.
`config.txt` is committed with the text "good content".
