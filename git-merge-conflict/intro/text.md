# `CONFLICT (content): Merge conflict in site.txt`

A `git merge feature` stopped halfway — `main` set the color to `red`, the branch
set it to `green`, and Git can't pick. The repo is mid-merge. Resolve it (keep
**green**) and complete the merge.
