# Don't commit that secret

You ran `git add -A` and now `.env` — which holds an API key — is staged for the
next commit. Get it out of the commit and make sure it never sneaks in again.
`README.md` should still be committed.
