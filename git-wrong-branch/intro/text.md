# That commit shouldn't be on `main`

You committed "WIP feature" (adding `feature.txt`) straight to `main` — it was
meant for a feature branch. Move that commit onto a new `feature` branch and get
`main` back to where it was, without losing the work.
