You set a CPU request and created a HorizontalPodAutoscaler targeting it between 1 and 5 replicas. Honesty note: without a metrics-server and real traffic this single-node cluster cannot actually scale the Deployment, so we graded that the HPA object exists with a max target rather than observing a live replica change. In a real cluster with metrics-server installed, load above 50 percent CPU would drive new replicas automatically.
