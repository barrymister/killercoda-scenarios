# Namespaces carve up a cluster

A **namespace** is a virtual boundary inside one cluster — a place to group
resources (e.g. `dev` vs `prod`) with their own names, quotas, and access. Let's
make one and run something in it.
