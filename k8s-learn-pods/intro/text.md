# Pods — the smallest thing you run

A **Pod** wraps one (or a few) containers. It's the unit Kubernetes schedules
onto a node. In this walkthrough you'll create one, look inside it, read its
logs, and delete it. Move to the next step.
