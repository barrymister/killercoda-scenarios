You tainted the node and scheduled a Pod that tolerates it, proving how taints and tolerations reserve nodes for chosen workloads. Remember a toleration permits scheduling but does not force it; pair it with nodeAffinity when you want to require placement. To clean up the taint later, re-run the taint command with a trailing minus, for example `kubectl taint nodes <node> dedicated=team:NoSchedule-`.
