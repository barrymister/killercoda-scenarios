You authored a StorageClass named `fast` that describes how storage should be dynamically provisioned. Honesty note: this single-node backend has no external provisioner, so no PV is actually created on demand - the lab grades that the StorageClass object exists, not live provisioning. On a real cluster with a working provisioner, a PVC naming `fast` would trigger an auto-created PV.
