# `greet: No such file or directory`

The `greet` command is installed in your PATH, but running it fails with **No
such file or directory** — even though `which greet` finds it. Something's off
with where it points.
