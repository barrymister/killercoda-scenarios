# Navigation, down

`pwd` says where you are, `cd` moves you, `ls -la` lists everything (including
hidden dotfiles), `cat`/`less` read files, and `find` locates them. Redirecting
with `>` created a new file. This is the ground floor of everything else.
