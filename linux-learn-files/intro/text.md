# Everything is a file, somewhere

The Linux filesystem is a tree starting at `/`. You move around it, list what's
there, and read files — all from the shell. A small `project/` folder is waiting
in your home directory. Move to the next step.
