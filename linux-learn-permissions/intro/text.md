# Who can read, write, run?

Every file has permission bits for its **owner**, **group**, and **others** —
read (r), write (w), execute (x). A script won't run without the x bit. There's
a `run.sh` in your home directory that isn't executable yet.
