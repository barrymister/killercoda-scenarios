# Pipes & filters

`grep` searched, `|` fed one command's output into the next, `wc -l` counted
lines, and `>` redirected the result into a file (`>>` appends). Chain `grep`,
`cut`, `sort`, `uniq`, `awk` and you can answer almost any "what's in this log?"
question without opening an editor.
