# Small tools, piped together

The Unix way: tiny programs that each do one thing, chained with `|`. You'll
search a log with `grep`, count with `wc`, and redirect the result into a new
file. There's an `access.log` in your home directory.
