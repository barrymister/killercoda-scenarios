# bench.service won't come up

A unit called `bench.service` is installed and enabled, but the service is dead —
`systemctl status bench.service` shows it **failed**. Work out why it can't start
and get it running.
