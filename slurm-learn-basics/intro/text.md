# What is a job scheduler?

On a shared GPU/HPC cluster you don't run programs directly — you ask a
**scheduler** to run them for you, when resources are free. **Slurm** is the
scheduler that runs most of the world's GPU clusters.

A real single-node Slurm cluster is starting up for you. Give it a few seconds,
then move to the next step.
